<UserControl x:Class="对比视频播放器.VideoPlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"
             MouseLeftButtonDown="UserControl_MouseLeftButtonDown"
             MouseWheel="UserControl_MouseWheel"
             MouseMove="UserControl_MouseMove">
    <Grid Background="Black">
        <MediaElement x:Name="MediaPlayer" 
                      LoadedBehavior="Manual" 
                      UnloadedBehavior="Stop"
                      MediaOpened="MediaPlayer_MediaOpened"
                      MediaEnded="MediaPlayer_MediaEnded"
                      MediaFailed="MediaPlayer_MediaFailed" />
        
        <Rectangle x:Name="BrightnessOverlay" Fill="White" Opacity="0" IsHitTestVisible="False" />
        
        <Rectangle x:Name="DarknessOverlay" Fill="Black" Opacity="0" IsHitTestVisible="False" />
        
        <Grid x:Name="EmptyState" Visibility="Visible">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="点击选择视频文件" Foreground="White" FontSize="18" TextAlignment="Center" />
            </Grid>
        </Grid>
        
        <Border x:Name="FeedbackOverlay" Visibility="Hidden" 
                Background="#88000000" CornerRadius="8" Padding="10"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="FeedbackText" Foreground="White" FontSize="16" />
        </Border>
        
        <Grid VerticalAlignment="Bottom" Opacity="0" x:Name="BottomControls">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <Slider x:Name="ProgressSlider" Grid.Row="0" Margin="5" Height="20" 
                    Background="Transparent" Foreground="White" Minimum="0" Maximum="100"
                    Thumb.DragStarted="ProgressSlider_DragStarted"
                    Thumb.DragCompleted="ProgressSlider_DragCompleted"
                    MouseLeftButtonUp="ProgressSlider_MouseLeftButtonUp" />
        </Grid>
    </Grid>
</UserControl>